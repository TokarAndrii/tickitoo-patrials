import { defineMessages } from "react-intl";

const compoundTripMessages = defineMessages({
  chooseButtonText: {
    id: "compoundTrip.compoundTripMessages.chooseButtonTextMessage",
    description: "text at choose seats button near prices of trip",
    defaultMessage: "Выбрать"
  }
});

export default compoundTripMessages;
