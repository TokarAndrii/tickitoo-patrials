import { defineMessages } from "react-intl";

const passengerMessages = defineMessages({
    seatMessage: {
        id: "passenger.passengerMessages.passengerSeatMessages",
        description: "text at passenger component before seat number (mean seat)",
        defaultMessage: "место"
    },
});


export default passengerMessages;