import { defineMessages } from "react-intl";

const bannerMessages = defineMessages({
  titleText: {
    id: "banner.bannerMessages.titleTextMessage",
    description: "text at banner title",
    defaultMessage: "ОФОРМЛЕНИЕ БИЛЕТОВ"
  },
  descriptionText: {
    id: "banner.bannerMessages.descriptionTextMessage",
    description: "text at banner description under the title",
    defaultMessage: "Шаг 2 из 3"
  }
});

export default bannerMessages;
